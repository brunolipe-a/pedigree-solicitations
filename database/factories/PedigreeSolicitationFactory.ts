import PedigreeSolicitation from 'App/Models/PedigreeSolicitation'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(PedigreeSolicitation, ({ faker }) => {
  return {
    //
  }
}).build()
